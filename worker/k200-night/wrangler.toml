name = "k200-night"
main = "src/worker.mjs"
compatibility_date = "2026-08-01"

# npx wrangler kv namespace create K200 로 만든 것. (네임스페이스 id 는 비밀값이 아니다)
[[kv_namespaces]]
binding = "K200"
id = "8eedd21723054d4c841bfe34236d4c5b"

# RELAY_TOKEN 은 코드/설정에 두지 않는다 — npx wrangler secret put RELAY_TOKEN
